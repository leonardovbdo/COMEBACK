// Evento Destroy do objeto obj_player
/*
if ds_exists(global.lines, ds_type_list) {
    for (var i = 0; i < ds_list_size(global.lines); i++) {
        var line_info = ds_list_find_value(global.lines, i);
        ds_map_destroy(line_info);
    }
    ds_list_destroy(global.lines);
}
*/